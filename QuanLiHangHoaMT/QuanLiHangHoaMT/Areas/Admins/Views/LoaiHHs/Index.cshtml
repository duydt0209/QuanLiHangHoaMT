@model IEnumerable<QuanLiHangHoaMT.Models.LoaiHH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admins/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiSP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiSP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiHH }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiHH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiHH })
        </td>
    </tr>
}

</table>
